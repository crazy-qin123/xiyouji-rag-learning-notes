    #`urllib.request` —— Python **标准库**的 HTTP 客户端，发网络请求不用装第三方包。
    #`.Request(...)` —— 创建一个 "请求对象" 存进 `req`。
    #`BOOK_URL` —— 变量，保存下载地址 (材料开头定义)
    #`headers={...}` —— 加 "请求头",`{...}` 是**字典**(键值对)。
    #`"User-Agent": "Mozilla/5.0"` —— 告诉服务器 "我是浏览器"。很多网站会拒绝 "裸奔" 的脚本请求，伪装一下更容易下载成功。